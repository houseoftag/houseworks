## Housework Updates:

### CRM:
- Clients should be a display_name, and should have Contacts, which are the people within that Client.
- The create button in the new client modal doesn't work
- The dashboard view is blank
- CRM integrations should be under Settings, not a tab in the CRM view
- The CRM view should be full-width, let's not waste space
- When the CRM view is selected, Dashboard stays highlighted in the sidebar, this is bad UX

### Workspaces:
- Create workspace fails with an error.